<%= form_with(model: [publication, comment]) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>
      <ul>
      <% comment.errors.full_messages.each do |message| %>
      <% if comment.id.present? %>
<li>
  <div class="comment-content" id ="this_comment_<%= comment.id %>"></div>
  <p><%= comment.content %></p>
  <p><%= link_to "Edit comment", edit_publication_comment_path(publication, comment), remote: true %></p>
  <!-- Addendum -->
  <p><%= link_to "Delete comment", publication_comment_path(comment.publication_id, comment.id), method: :delete, remote: true,
                                  data: { confirm: "Do you really want to delete this?"} %></p>
  <!-- So far -->
</li>

      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.text_area :content, id: "comment_content_#{publication.id}" %>
    <%= form.label :content %>
  </div>
  <div class="field">
    <%= form.text_field :name, id: "comment_name_#{publication.id}" %>
    <%= form.label :name %>
  </div>
  <div class="field">
    <%= form.text_field :email, id: "comment_email_#{publication.id}" %>
    <%= form.label :email %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
