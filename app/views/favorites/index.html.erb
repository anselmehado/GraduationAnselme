<p id="notice"><%= notice %></p>

<h1>Favorites</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @favorites.each do |favorite| %>
    <div class="row">
       <div class="col-md-6">
         <%= image_tag favorite.publication.pub_image, style:'width:300px;height:300px' %>
       </div>
       <div class="col-md-6">
          <div class="full blog_cont">
             <h4><%= favorite.publication.title %> Author :<h5><%= favorite.publication.user.name %></h5></h4>
             <h5><%= favorite.publication.date %></h5>
             <h5><%= favorite.publication.category.name %></h5>
              <h5><%= favorite.publication.category.name %></h5>
             <p>T<%= favorite.publication.content %></p>

             <div class="button_section">
               <%= link_to 'Read', favorite.publication %>


             </div>
          </div>
       </div>
    </div>

    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Favorite', new_favorite_path %>
